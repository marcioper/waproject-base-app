lane :release do
  # get_certificates           # invokes cert
  # get_provisioning_profile   # invokes sigh
  build_app(scheme: "reactAppRelease",
            workspace: "reactApp.xcworkspace",
            include_bitcode: true)
end